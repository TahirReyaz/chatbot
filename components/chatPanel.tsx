import { Textarea } from "./ui/textarea";

const ChatPanel = () => {
  return (
    <div>
      <div className="text-gray-50">Messages</div>
      <Textarea />
    </div>
  );
};

export default ChatPanel;
